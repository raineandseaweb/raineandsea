# ==============================================
# 🕵️ SENSITIVE SECRETS (MOVED TO GCP SECRET MANAGER)
# ==============================================
# The following secrets should be stored in GCP Secret Manager:
# - DATABASE_URL / POSTGRES_URL
# - KV_REST_API_URL / KV_REST_API_TOKEN
# - BLOB_READ_WRITE_TOKEN
# - CLOUDFLARE_ACCESS_KEY_ID / CLOUDFLARE_SECRET_ACCESS_KEY / CLOUDFLARE_R2_API_TOKEN
# - STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET
# - ALGOLIA_ADMIN_API_KEY
# - RESEND_API_KEY / SMTP_USER / SMTP_PASS / GMAIL_PASSWORD / FROM_EMAIL
# - NEXTAUTH_SECRET / JWT_SECRET / CSRF_SECRET / ROOT_SECRET
# - TAXJAR_API_KEY / SHIPPO_API_KEY
# - CLOUDFLARE_ACCOUNT_ID (if not public)
# - GCP_SERVICE_ACCOUNT_KEY_PATH

# ==============================================
# 🌐 PUBLIC / NON-SENSITIVE (KEEP IN .env.local)
# ==============================================

# Stripe (Public Keys)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Algolia (Public Keys)
ALGOLIA_APP_ID=
NEXT_PUBLIC_ALGOLIA_SEARCH_KEY=

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME="Ecommerce Store"
SITE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# SMTP Configuration (Non-sensitive)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Cloudflare R2 (Public Config)
CLOUDFLARE_R2_PRODUCTS_BUCKET=products
PUBLIC_URL_BASE=https://pub-xxxxx.r2.dev

# GCP Configuration (Infrastructure Config)
GCP_PROJECT_ID=
GCP_SERVICE_ACCOUNT_KEY_PATH=.secrets/service-account.json

# ==============================================
# 📝 SETUP INSTRUCTIONS
# ==============================================
# 1. Copy this file to .env.local
# 2. Fill in the public/non-sensitive values above
# 3. Run 'npm run secrets:migrate' to migrate secrets to GCP Secret Manager
# 4. See GCP_SECRET_MANAGER_SETUP.md for detailed instructions
